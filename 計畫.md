# Breeze Elf — 未完成項目

> 2026-07-28 更新。這份是**下一個對話的起點**：只記「還沒做的」與「做之前必須知道的」。
> 已完成的細節在 `TRAINING_PLAN.md`（唱歌辨識／LoRA 後訓練，含執行紀錄）、
> `ROADMAP.md`（#1–#5 全數完成）、`OPTIMIZATION_PLAN.md`（效能，P0–P2 全完成）。

---

## 現況一句話

簡譜這條線已經完整（調性 → 歌詞與時間軸 → 音符時值 → 音準分數 → **主檢視的時值符號**），
台語 LoRA 已訓完並上線成可切換 preset（nan CER 1.0963 → 0.6614，相對降 39.7%）。
評測的**程式**已經一次量五個指標、C 層負樣本的**產生管線**也接好了，
**剩下的是：這兩件事都還沒在真機上跑過數字、免訓練的旋鈕從沒掃過、英文線資料不足。**

| 已完成 | 位置 |
|---|---|
| 調性偵測取代中位數當主音 | `audio.py:estimate_tonic` |
| 歌詞對齊（貼歌詞 → 取代聽錯的字、沿用量到的時間軸） | `lyrics.py` / `POST /api/transcript/lyrics` |
| 拍子估計 + 音符時值量化 | `audio.py:estimate_tempo` / `quantize_beats` |
| 音準評分（整體／逐句／逐字） | `audio.py:score_intonation` |
| 資料集建置 → LoRA → merge/CT2 → 部署成 preset | `breeze_elf/dataset_builder.py`、`tools/*.py` |
| 台語 LoRA v1/v2 上線、實機 A/B 過 | `models/presets.json`、`tools/deploy_model.py` |
| **簡譜時值符號（減時線／附點／延長線）** | `web/app.js:renderJianpuLine` + `index.html` CSS |
| **拍號推定與小節線** | `audio.py:estimate_meter` / `bar_before_flags` + `renderJianpuLine` |
| **評測五指標的量測程式**（純函式，免 GPU 可單測） | `tools/eval_asr.py` |
| **C 層負樣本產生與入庫** | `dataset_builder.instrumental_chunks` / `tools/make_manifests.py` |
| **幻覺閘門抽成共用模組**（評測量到的就是產品在跑的那個） | `breeze_elf/hallucination.py` |

---

## A. 評測：程式好了，**數字還沒跑**（**下一步就是這個**）

`tools/eval_asr.py` 現在一次輸出 CER/WER/MER、幻覺率、對齊誤差、RTF、說話回歸，
報告寫進 `dataset/eval_reports/<tag>.json`，並在最後列出「這一輪沒量到的指標與原因」。
但這些都還沒有在有 GPU、有 `dataset/` 的機器上跑過，所以**baseline 表仍是空的**。

要跑之前還缺三樣輸入：

| 缺的輸入 | 怎麼補 |
|---|---|
| **C 層負樣本的音檔** | 用 `--negative_ratio`（預設 0.08）重建有伴奏的歌：`build_lyric_dataset.py … --source_type youtube/local`。清唱與朗讀語料不會產生負樣本（`is_mix=False`），這是刻意的 |
| **`speech_` 前綴的說話回歸集** | 20 段會議錄音，用 `--source_type local` 逐檔進 metadata，id 開頭必須是 `speech_` |
| **人工標註的時間軸** | 在 `dataset/manifests/*.jsonl` 想量的那幾筆加上 `words: [{word, start, end}]`；沒標註的 clip 不會被算進對齊誤差，報告會告訴你它沒量到 |

跑法：

```
.venv/Scripts/python.exe tools/eval_asr.py \
    --model models/breeze-asr-25-ct2 --sources nan,mir1k,negative,speech --tag breeze_baseline
```

驗收：`dataset/eval_reports/breeze_baseline.json` 五個數字齊全，且原始 preset 與台語 LoRA
各跑一份可以對照。

---

## B. 免訓練的上限：`TRAINING_PLAN §2` 一格都沒掃

`eval_asr.py` 的解碼刻意對齊 production（`beam_size=1`、`vad_filter=False`、
`condition_on_previous_text=False`），也就是說**整張旋鈕表還是原始設定**。這件事的順位其實
在微調之前：若某個旋鈕給的增益大於 LoRA，就該直接做成產品設定。

待掃（每項單獨量增益）：Demucs 強制開 + `htdemucs` vs `htdemucs_ft`、檔案模式 `beam_size=5`、
`temperature` fallback、`compression_ratio_threshold`、檔案模式 `condition_on_previous_text=True`、
「以下是歌詞」風格的 `initial_prompt`、**針對分離後人聲重掃 `no_speech_prob`/RMS 門檻**
（現行 0.6 / 0.02 是對說話調的）、int8 vs int8_float16 vs float16。

`--beam` 已經是 CLI 參數；門檻那一項也已經接成 `--no-speech-threshold` / `--rms-threshold`
（預設讀現行 config），所以掃描不需要改程式，只要換旗標重跑並比報告。
其餘旋鈕（Demucs、temperature、initial_prompt、compute_type）目前還得改 `transcribe_one`。

---

## C. 訓練線：接續 `TRAINING_PLAN` 附錄的待辦

- **英文線（B 線）卡在資料**。whisper-medium en LoRA 已訓過，dev loss 1.60→0.724 但
  test WER 0.788→0.798（持平略退，16 首訓練歌過擬），**已決定不部署**、adapter 留檔。
  第二輪需要更多英文歌唱資料 —— 這是資料問題，別在超參上耗。
- **CV zh-TW 入庫** → 混 5% 防遺忘（`--dataset_name common_voice --target_lang zh-TW`）。
- **台語歌 YouTube + LRC** → 第二輪台語歌唱 LoRA（`--source_type youtube --lyrics *.lrc --target_lang nan`）。
  目前的台語 LoRA 訓的是 Common Voice **說話**，唱歌只有 MIR-1K 的華語。
  這一輪順便就會產出 C 層負樣本（有伴奏的歌才有間奏）。
- **TAT-Vol1** 授權核可後入庫。

---

## D. 簡譜與音樂分析的收尾

- ~~主檢視還沒有時值符號~~ ✅ 已完成（2026-07-28）：`renderJianpuLine` 依 `beats` 畫減時線
  （八分一條、十六分兩條）、附點、延長線（`1 - -` 各占一格，所以歌詞那列在延長線底下留白）。
  時值表是 `audio.py:_NOTE_VALUES` 的同一組值，用 log 空間找最近的一格。
- ~~沒有拍號與小節線~~ ✅ 已完成（2026-07-28）：`audio.py:estimate_meter` 把每拍的 onset
  能量相加、折在 2/3/4 拍找出下拍（`_meter_from_accents`），信心不足就誠實回退 4/4；payload
  帶 `tempoPhaseSeconds`/`beatsPerMeasure`/`meterConfidence`，每字帶 `barBefore`
  （`bar_before_flags` 依 phase＋下拍偏移算小節邊界），`renderJianpuLine` 在新小節首音前畫
  `.jianpu-barline`。拍號測不準（如 2/4 的 loud-soft）時統計列標「?」表示是假設而非量到。
  精修方向：拍號只判 2/3/4，且 `_beat_phase` 找的是拍相位不是小節下拍（靠 `downbeat_offset`
  補），要更準得引小節級 autocorrelation 或和聲變化點。
- **快歌可能回報成半速**（先驗選靠近 120 BPM 的解）。這是刻意的，但 UI 可以給一個
  「×2 / ÷2」的手動修正。
- **即時逐句的主音會跳**：每句各自估計，句與句之間不一致（後處理才有全域主音）。
  可在 `StreamState` 累積 session 級的音高直方圖。
- **歌詞對齊：整句漏掉時只能平均內插**（`anchoredRatio` 會標示）。精修方式是改用
  ctranslate2 的 `Whisper.align`（4.7.2 已確認存在，`faster_whisper` 也公開了
  `find_alignment`）做逐 token 強制對齊，就能量出真正的時間。
  做完之後 A 的對齊誤差就有第二個對照組。
- **音準量不到「唱錯音」**（無參考旋律），有伴奏時也對不到伴奏的調音。若要做，參考旋律得
  從歌詞對齊之外另外來源。

---

## E. 實測與環境

- `OPTIMIZATION_PLAN.md` 的 **手機端實測**（P0-2 背壓觀測、P0-3 前端背壓保護、P2-1 前端體驗）
  與 **實際並發串流實測**（P1-2 ASR queue）到現在都還掛著「待實測」。
- `requires-python` 已提到 **>=3.11**（onnxruntime 從 1.24 起不再發 cp310 wheel，而它是
  faster-whisper 的核心依賴）。本機若還在 3.10，`uv sync` 會拒絕。

---

## 開工前必讀的陷阱

1. **訓練／評估一律用 `.venv/Scripts/python.exe` 直呼，不要 `uv run`** —— `uv run` 會 sync 回
   `tokenizers` 0.23.1 而炸掉 transformers（訓練堆疊釘 0.22.2）。
2. **preset 有兩條註冊路徑**：`BREEZE_ASR_BREEZE_NAN_MODEL` 這類 env-var 閘控的內建 preset 在
   **process 啟動時**讀取（手機連的 server 要重啟才看得到）；`tools/deploy_model.py` 寫進
   `models/presets.json` 的動態註冊則**每個 request 重讀，免重啟**。要讓手機立刻看到就走後者。
3. **永遠不要覆蓋 `models/breeze-asr-25-ct2`** —— 原始與微調版都要留著才能 A/B。
4. **評估正規化會決定結論**：MIR-1K 歌詞無標點而 Breeze 輸出有標點，不正規化就會高估 CER；
   Common Voice nan 的台羅註解佔標籤字元 48%，不剝掉會同時毀掉訓練與評估
   （`tools/text_norm.py` 已處理，改規則時記得用 `tools/rescore.py` 回溯重算，不必重跑解碼）。
   **但 `rescore.py` 只能重算文字距離**：幻覺率要讀音檔的 RMS 與模型的 `no_speech_prob`，
   對齊誤差要時間戳，RTF 要解碼耗時 —— 這三個一律要重跑 `eval_asr.py`。
5. **CI 跑 `uv sync --extra dev` → `ruff check .` → `unittest discover`（帶 `BREEZE_ASR_PROVIDER=mock`）
   → `uv build --wheel`**。改動前先在本機照這個順序跑一遍，特別是那個環境變數 —— 它會讓
   任何依賴 `get_settings()` 預設 provider 的測試拿到 MockASR。
6. **`unittest discover` 只收 `TestCase`**：`tests/test_dataset_builder.py` 是 pytest 風格的
   module-level function，所以那一整檔在 CI 裡是**沒有跑到的**（`pytest` 收 438 個，
   `unittest` 收 427 個）。新測試請寫成 `unittest.TestCase`，兩邊都會收。
7. **幻覺判定只有一份**，在 `breeze_elf/hallucination.py`。改門檻或改 pattern 就同時改到產品與
   評測；不要在工具裡再抄一份，那正是「量到的不是上線那個」的起點。

---

## 建議順序

1. **A（在有 GPU 的機器上跑出 baseline 五個數字）** —— 程式已經好了，缺的是負樣本音檔、
   說話回歸集、跟人工時間軸標註。沒有這張表，後面任何改動都無法判斷有沒有變好。
2. **B（免訓練旋鈕掃描）** —— 最便宜，可能吃掉一半增益。門檻與 beam 已經是旗標，可直接開掃。
3. ~~**D 的第二項（拍號與小節線）**~~ ✅ 已完成（2026-07-28，見 D 節）。D 剩下的（拍號精修、
   快歌半速手動 ×2/÷2、逐 token 強制對齊）投報率較低，等 A/B 的 baseline 出來再排。
4. **C（第二輪訓練）** —— 等 1、2 給出新的 baseline 之後再決定要不要投。
